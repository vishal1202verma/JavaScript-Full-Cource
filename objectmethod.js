let emp = {
    id:101,
    name:"vishal",
    age:20,


}
//  fecthing the keys of an object 

let keys = object.keys(emp);
console.log(keys);

//  getting all values of an object 

let values = object.values(emp);
console.log(values);

//  keys and values fetching 
 let entries = object.entries(emp);
 console.log(entries);